@*@model WebUI.Models.ProductsListViewModel*@

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/bootstrap")
    <script src="jquery-2.2.3.min.js"></script>
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
        <div class="container-fluid">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="#">Yapart</a>
                @*@Html.ActionLink("Application name", "Index", "Home", null, new { @class = "navbar-brand" })*@
            </div>
            <div class="navbar-collapse collapse">
                <h4 class="nav navbar-nav navbar-right">
                      @if (Request.IsAuthenticated) 
                      {
                          @*using (Html.BeginForm("LoggedOff", "Account", FormMethod.Post, new { id = "logoutForm" }))
                          {
                              <a href="javascript:documet.getElementById('logoutForm').submit()">Logout</a>
                          }*@
                          @Html.ActionLink("Logout", "Logout", "Account")
                      }
                      
        
                </h4>
                <h4 class="navbar-form navbar-right">
                    @if (Request.IsAuthenticated)
                    {
                        @Html.ActionLink(User.Identity.Name, "Setting", "ProfileSettings")
                    }

                </h4>

                
            </div>
        </div>
    </div>
    <br /> <br />
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-3 col-md-2 sidebar">
                <ul class="nav nav-sidebar">
                    <li class="active" role="presentation">
                        @Html.ActionLink("Home", "Index", "Home")
                    </li>
                    <li>
                        @Html.ActionLink("Product", "List", "Product")
                    </li>
                    
                </ul>
                <br /> <br />
                <ul class="nav nav-sidebar">
                    <li>
                        @Html.ActionLink("Информация о заказах", "List", "Order")
                    </li>
                    <li>
                        @Html.ActionLink("Поставщикам", "Index", "Provider")
                    </li>
                    <li>
                        @Html.ActionLink("Контакты", "EmployeesList", "Employee")
                    </li>
                    <li>
                        @Html.ActionLink("Настройка", "Index", "Home")
                    </li>
                    
                </ul>
                               
            </div>
            <div class="col-sm-9">
                  @RenderBody()

        </div>

        </div>
   </div>
    <div class="container body-content">
        <footer>
            <p>© @DateTime.Now.Year - My ASP.NET Application</p>
        </footer>
        </div>


        @Scripts.Render("~/bundles/jquery")
        @Scripts.Render("~/bundles/bootstrap")
        @RenderSection("scripts", required: false)
</body>
</html>
